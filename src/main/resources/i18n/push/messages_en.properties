# i18n keys for push notification message text (used by PushMessageFactory / test push).
# - Adding a scenario: add push.message.{id}.title and push.message.{id}.body (Markdown source) here and in the zh file.
# - body uses lightweight Markdown (**bold** / links / line breaks \n); {{key}} placeholders match the mail template's keys.
# - Only push message text lives here; logs / config.yaml comments stay in messages.properties.
# - Title and body never contain cookie / PHPSESSID / any credential.

# ── Test push (GUI "Test this channel" button) ──────────────────────────────
push.test.message.title=Pixiv Downloader test push
push.test.message.body=If you received this **formatted** message, this push channel is configured correctly.

# ── Scheduled-task auto-suspend scenarios (paired with mail templates; same id and placeholders) ──
push.message.overuse-paused.title=⚠️ Overuse warning detected, scheduled tasks paused
push.message.overuse-paused.body=Pixiv sent an "overuse" warning to account **{{account_id}}**, and **{{tasks_count}}** scheduled task(s) for it have been paused automatically.\n\n- Warning time: {{warning_time}}\n- Paused at: {{trigger_time}}\n\nAffected tasks:\n{{tasks_list_md}}\n\nResume from the "Scheduled Tasks" page after assessing the risk.
push.message.auth-expired.title=🔑 Scheduled task auth expired, re-authorization required
push.message.auth-expired.body=The login session (cookie) bound to scheduled task **{{task_name}}** (ID {{task_id}}) has expired, and the task has been suspended automatically.\n\n- Task type: {{task_type}}\n- Trigger: {{task_trigger}}\n- Suspended at: {{trigger_time}}\n- Next scheduled run: {{next_run_time}}\n\nRe-authorize the cookie on the "Scheduled Tasks" page; it will resume and retry the isolated works first.
push.message.circuit-breaker.title=🛑 Scheduled task circuit-broken
push.message.circuit-breaker.body=Scheduled task **{{task_name}}** (ID {{task_id}}) failed **{{consecutive_failures}}** times in a row and tripped the circuit breaker, so it has been suspended.\n\n- Task type: {{task_type}}\n- Trigger: {{task_trigger}}\n- Suspended at: {{trigger_time}}\n- Next scheduled run: {{next_run_time}}\n- Last failure: {{last_error_excerpt}}\n\nCheck and re-authorize the cookie on the "Scheduled Tasks" page.
push.message.pending-exhausted.title=🧩 Work exhausted auto-retry, manual action required
push.message.pending-exhausted.body=The {{work_kind}} work **{{work_id}}** in scheduled task **{{task_name}}** (ID {{task_id}}) still fails after **{{attempts}}** automatic retries, so auto-retry has stopped.\n\n- Task type: {{task_type}}\n- Trigger: {{task_trigger}}\n- Work link: {{work_url}}\n- Triggered at: {{trigger_time}}\n- Next scheduled run: {{next_run_time}}\n- Last failure: {{last_error_excerpt}}\n\nOpen the "🧩 Pending retries" panel on the "Scheduled Tasks" page to handle it manually.
