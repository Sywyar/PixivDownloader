package top.sywyar.pixivdownload.plugin.api.gui;

/**
 * Value coercion used when a GUI config action copies field values into its request payload.
 */
public enum GuiConfigActionPayloadType {
    STRING,
    INT,
    BOOLEAN
}
