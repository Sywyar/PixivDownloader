package top.sywyar.pixivdownload.core.gallery.model.media;

/** Concrete asset kind returned by a work detail provider. */
public enum GalleryMediaKind {
    IMAGE,
    VIDEO,
    LIVE_PHOTO_VIDEO,
    UGOIRA,
    COVER,
    TEXT
}
