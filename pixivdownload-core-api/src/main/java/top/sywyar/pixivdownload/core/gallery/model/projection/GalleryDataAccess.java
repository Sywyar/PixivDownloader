package top.sywyar.pixivdownload.core.gallery.model.projection;

/** Dataset-level access boundary declared by a source projection provider. */
public enum GalleryDataAccess {
    SHARED,
    ADMIN_ONLY
}
