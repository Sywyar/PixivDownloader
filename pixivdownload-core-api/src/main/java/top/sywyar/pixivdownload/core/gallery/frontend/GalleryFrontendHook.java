package top.sywyar.pixivdownload.core.gallery.frontend;

/** Stable gallery extension hooks implemented by a trusted same-origin plugin module. */
public enum GalleryFrontendHook {
    VIEW_ENTRY,
    FILTER_EXTENSION,
    CARD_EXTENSION,
    MEDIA_RENDERER,
    DETAIL_ACTION
}
